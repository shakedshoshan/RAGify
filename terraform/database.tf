# Database table configuration removed for minimal setup
# Tables can be created manually after RDS instance is provisioned
# This allows for a simpler Terraform configuration without PostgreSQL provider dependencies
